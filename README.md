# dotfiles
My collection of terminal configuration files and docs

The project links to other source files using submodules, initialize them with the submodule command

```
git submodule update --init --recursive
```

And fetch them via: `git submodule update --recursive`

## macOS additional installations
- [command-line fuzzy finder](https://github.com/junegunn/fzf)
